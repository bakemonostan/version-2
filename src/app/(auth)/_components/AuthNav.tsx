import { LogoIcon } from "@/components/icons";

export default function AuthNav() {
  return (
    <header className="border-b border-gray-200">
      <nav className="flex-between landing-container p-5">
        <LogoIcon />
      </nav>
    </header>
  );
}
